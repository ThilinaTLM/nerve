---
title: Navigate the workbench
description: Use tabs, movable panels, project navigation, search, and responsive layouts.
sidebar:
  order: 1
---

import DocsScreenshot from "../../../components/docs/DocsScreenshot.astro";
import conversationDark from "../../../assets/screenshots/desktop/conversation-dark.webp";
import conversationLight from "../../../assets/screenshots/desktop/conversation-light.webp";
import rightSheetDark from "../../../assets/screenshots/mobile/right-sheet-dark.webp";
import rightSheetLight from "../../../assets/screenshots/mobile/right-sheet-light.webp";

The desktop layout has a conversation/task dock, a tabbed center workspace, and a secondary dock. Drag resize handles to allocate space. Panel views can move among left, right, and bottom docks or be hidden; Nerve repairs obsolete saved layouts after upgrades. The title-bar Help button opens the built-in Guides catalog, including a full Workbench tour.

## Dock views

Seven views can live in docks:

- **Conversations**, **Files**, **Tasks**, and **Notes** begin on the left.
- **Git**, **Pull Requests**, and **Context** begin on the right.

Notes are a project panel, not a center tab. Panels intentionally adapt to their current width rather than assuming a particular dock.

## Center tabs

The center workspace opens conversations, pending conversations, task output, file previews, pull request details, Settings, authentication, and Nerve logs. Reorder tabs or close the current tab, tabs to either side, or all others. Conversations stay mounted when hidden so live transcript state remains stable.

## Switch projects and find work

The title bar keeps up to five recent projects in reach on desktop, two in compact layouts, and one on phones. Open the project switcher for the complete recent list. It searches project names and directory paths, supports arrow keys plus Enter or Space, marks the current project, and shows activity from conversations and running tasks. **Browse for a project** opens the full Recent/Browse directory picker for a location that is not already known to Nerve.

Project menus provide shortcuts for a new conversation, an external editor or terminal, conversation pruning, and removing the Nerve project record. Removing a record does not delete the source directory.

Conversation search is a separate modal and filters the selected project's conversations. The navigator initially bounds long lists and exposes more entries on demand. Switching projects changes the selected scope without deleting server-side conversations or tasks.

<DocsScreenshot
  light={conversationLight}
  dark={conversationDark}
  alt="The Nerve desktop workbench showing the conversation navigator, a transcript with tool calls and their output, and the Git panel"
  caption="What to notice: navigation, transcript, and project panels share one shell. Dock views can move or hide without interrupting the active conversation."
  widths={[480, 768, 1024, 1440]}
  sizes="(min-width: 72rem) 48rem, calc(100vw - 3rem)"
/>

## Compact layouts

Below 1024px, Nerve switches to a single center column and moves dock views into sheets. Below 640px, a phone-native shell replaces the desktop docks: **Inbox**, **Chats**, **Workspace**, and **More** stay in the thumb-zone tab bar, while conversations and workspace tools open as focused detail screens. This is a functional layout, not merely a scaled desktop screenshot.

Layout and active-tab preferences are browser-local. A different browser profile or cleared site storage starts with defaults even though server-side projects and conversations remain.

<DocsScreenshot
  light={rightSheetLight}
  dark={rightSheetDark}
  alt="The Nerve phone layout with the Git Changes detail screen opened from the Workspace tab"
  caption="At phone width, the Workspace tab keeps Git, pull requests, files, tasks, context, and notes reachable without compressing the conversation."
  widths={[390, 585, 780, 1170]}
  sizes="min(24rem, calc(100vw - 3rem))"
  variant="phone"
/>

## Built-in tour

Choose **Help → Guides → Work through the Workbench** for an interactive tour of the composer, review controls, panels, files, Git, tasks, providers, Settings, and status. The tour needs an active project and can be replayed after completion.

## Next steps

- [Conversations](/guides/conversations/)
- [Edit files, inspect context, and keep notes](/guides/files-context-notes/)
- [Agents and delegation](/guides/agents-and-delegation/)
- [Themes, zoom, and shortcuts](/guides/personalize/)
- [Configure Settings](/guides/settings/)
- [Use the built-in Guides](/start/guides/)
